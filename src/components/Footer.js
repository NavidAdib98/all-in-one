import React from "react";
import { Container, Nav, Navbar } from "react-bootstrap";

const Footer = () => {
  return (
    <Navbar bg="dark" variant="dark" style={{ height: "200px" }}>
      <Container>
        <Navbar.Brand>Thank you very much for watching us</Navbar.Brand>
      </Container>
    </Navbar>
  );
};

export default Footer;
